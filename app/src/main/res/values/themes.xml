<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base de la app (Views) basado en Material 3 -->
    <style name="Theme.InDriveAutopilot" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Personalización mínima de ejemplo -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <!-- Si usas MaterialToolbar, AppBar, etc. no necesitas ActionBar -->
    </style>

</resources>