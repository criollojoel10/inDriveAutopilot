<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InDriveAutopilot">

        <!-- SETTINGS como pantalla principal (abre el engranaje de LSPosed y el icono del launcher) -->
        <activity
            android:name="dev.joel.indriveautopilot.ui.SettingsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.InDriveAutopilot">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- (Opcional) Deja MainActivity sin launcher o elimínala si no la usas -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Requerido por Xposed/LSPosed (legacy) -->
        <meta-data android:name="xposedmodule" android:value="true"/>
        <meta-data android:name="xposeddescription" android:value="@string/xposed_desc"/>
        <meta-data android:name="xposedminversion" android:value="82"/>

        <!-- (Opcional, no activado aún) para preferencias legibles desde hooks sin hacks:
        <meta-data android:name="xposedsharedprefs" android:value="true"/>
        -->
    </application>
</manifest>
