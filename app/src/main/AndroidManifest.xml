<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InDriveAutopilot">

        <!-- SETTINGS como pantalla principal (abre el engranaje de LSPosed y el icono del launcher) -->
        <activity
            android:name="dev.joel.indriveautopilot.ui.SettingsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.InDriveAutopilot">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- (Opcional) Deja MainActivity sin launcher o elimínala si no la usas -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Mantener XSharedPreferences moderno (New XSharedPreferences / LSPosed) -->
        <meta-data android:name="xposedsharedprefs" android:value="true"/>
        <!-- <meta-data android:name="xposedscope" android:value="ru.indriver.android"/> -->
        <meta-data android:name="xposedscope" android:value="ru.indriver.android"/>
        
        <!-- NOTA: se han eliminado a propósito las meta-data legacy:
             xposedmodule, xposeddescription, xposedminversion
             LSPosed modern usará META-INF/lsposed/module.json en su lugar.
        -->
    </application>
</manifest>
